public class Main {

public static void main(String[] args);

public class Throw
{Exeption Exeptions;

{

{
    //TODO Auto-generated method stub
File dictionary = new File("http://www.gwicks.net/textlists/english3.zip");

Scanner textScanner = new Scanner (dictionary);
Scanner input = new Scanner(System.in);
   ArrayList<String> words = new ArrayList<>();
while(textScanner.hasNextLine()) {
words.add(textScanner.nextLine());
}

String_hidden_text = words.get((int)(Math.random()*words.size()));
char[] textArray = hidden_text.toCharArray ();

char [] myAnwsers = new char[textArray.length];
for(int i =0; i < textArray.length; i ++) {
myAnwsers [i] = '?';
}
boolean finished = false;
int lives=6;
while(finished == false) {
System.out.printIn("*****************");

String letter = input.next();
//check for valid input
while(letter.length () != 1 || Character.isDigit(letter.charAt(0))){
System.out.printIn("Error Input - Try Again");
letter = input.next();
}
//checks if letter is in the word
boolean found = false;
for(inti =0; i < textArray.length; i++) {
if(letter.charAt(0) == textArray [i] ){
myAnswers[i] = textArray[i];
found = true;
}
}
if (!found) {
lives--;
System.out.printIn("Wrong Letter");
}
boolean done = true;
for(int i = 0; i < myAnswers.length; i++){
if(myAnswers[i] =='?') {
System.out.printIn(" _");
done= false;
}
else{
System.out.printIn(" " + myAnswers [i]);
}
}
System.out.printIn("\n" + "Lives left: " +Lives);

//check if the game ends
if(done){
System.out.printIn("Good Job");
finished = true;
}
if(lives <= 0) {
Syste.out.printIn("Dead");
finished = true;
}
}

